#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
char l;

	for (l = 'a'; l <= 'z'; l++)

		_putchar(l);

	putchar('\n');

	print_alphabet();
    	return (0);

}
