#include "main.h"

/**
 * _strpbrk - check code
 * @s: cadena 1
 * @accept: cadena 2
 * Return: 0
 */

void print_diagsums(int *a, int size)
{
	int i;
	int j;
	int acum1 = 0;

	for (i = 0; i < size; i++);
	{
		for (j = 0; j < size; j++)
		{
			if (i = j)
			{
				acum1 = acum1 + a[i][j];
			}	

			

}
