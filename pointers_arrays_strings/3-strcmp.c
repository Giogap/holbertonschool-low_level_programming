#include "main.h"

/**
 * _strcmp - check the code
 * @s1: compare with s2
 * @s2: compare with s1
 * 
 * Description: compare
 * Return: s1 - s2
 */

int _strcmp(char *s1, char *s2)
{
	int i;

	for (i = 0; s1[i] == s2[i]; i++)
		
		return (s1[i]);
		return (s2[i]);
	
}
