#include <stdio.h>
#include "dog.h"

/**
  * init_dog - check code
  * @d: array info dog
  * @name: name of dog
  * @age: age of dog
  * @owner: owner of dog
  */

void print_dog(struct dog *d)
{
	if (d = NULL)
	{
		printf (nil);
	}
		d->name = name;
		d->age = age;
		d->owner = owner;
	if (name = NULL)
	{
		printf("(nil)\n");
	}
	else
	{
		printf("%s\n", d->name);
	}
	printf("%p\n", d->age)
	printf("%s\n", d->owner)	
}
