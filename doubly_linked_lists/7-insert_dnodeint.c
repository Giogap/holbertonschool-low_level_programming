#include "lists.h"
#include <stdlib.h>


dlistint_t *insert_dnodeint_at_index(dlistint_t **h, unsigned int idx, int n)
{
	dlistint_t *nnode, *temp = *h;
	unsigned int index, cont = 0;

	nnode = malloc(sizeof(dlintint_t));

}
